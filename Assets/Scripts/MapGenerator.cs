using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapGenerator : MonoBehaviour
{
    /*public GameObject techo;
    public GameObject piso;
    public GameObject prevtecho;
    public GameObject prevpiso;*/

    public GameObject player;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*Debug.Log(player.transform.position.x);
        if(player.transform.position.x > piso.transform.position.x)
        {
            var tmptecho = prevtecho;
            var tmppiso = prevpiso;
            prevtecho = techo;
            prevpiso = piso;
            tmptecho.transform.position += new Vector3(80, 0, 0);
            tmppiso.transform.position += new Vector3(80, 0, 0);
            techo = tmptecho;
            piso = tmppiso;

        }*/
    }
}
